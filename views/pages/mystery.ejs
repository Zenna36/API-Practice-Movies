<%- include ('../partials/header'); %>

<main class="main container">   
    <div class="row">
        <% data.forEach(item => { %>
            <div class="col-md-4">
                <a href="/mystery/<%= item.id %>">
                    <img class="img-fluid" src="<%= item.posterURL %>" alt="<%= item.title %>">
                    <h2><%= item.title %></h2>
                    <hr>
                </a>
            </div>
        <% }) %>
    </div>
</main>

<%- include ('../partials/header'); %>